#pragma once

#include "cVisitor.h"

// Stub for future labs (type/size computation).
// For Lab 5b, this is intentionally a no-op so the project can build.
class cComputeSize : public cVisitor
{
public:
    cComputeSize() = default;
};
